# Deny .htaccess, .htpasswd, .git, .gitignore, etc.
location ~ /\.(ht|git) {
    deny all;
}

# For crawlers
location = /robots.txt {
    allow all;
    access_log off;
    log_not_found off;
}

location = /favicon.ico {
    log_not_found off;
    access_log off;
}
